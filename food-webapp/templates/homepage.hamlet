<h1>Colledge Food

<ol>
  <li>
    <a href=@{RecipesR}>Recipes

  <hr />
  <li #form>
    $if imported
      <div>CSV imported
    $else
      <div>Not imported
    <form method=post action=@{HomeR}#form enctype=#{formEnctype}>
      ^{formWidget}
      <button .btn .btn-primary type="submit">
         Import CSV! <span class="glyphicon glyphicon-upload"></span>
  <hr />

<ol>
